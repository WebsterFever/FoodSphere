# Railway

FoodSphere uses Railway for the NestJS API, PostgreSQL, and Redis.

The PostgreSQL connection is supplied through `DATABASE_URL`. Never commit Railway credentials or connection strings.

Actual PostgreSQL + Prisma integration is Phase 2.
